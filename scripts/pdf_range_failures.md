# PDF 结构优化失败调查（2026-09-16）

## 数据来源与范围

线上状态来自 `vomebook/Reader-Assets` 固定 revision
`33db8c051a01182c1ed0ec567f37d8da240f2e8a`。这是调查起点，不能把后面的本地样本结果
当作线上已经更新的计数。调查阶段修复尚未发布；后续发布记录见文末。本地 qpdf 为 10.6.3，PDF.js 为 6.3.289，
pypdf 为 6.14.2。Runner 的 qpdf 版本可能不同，身份指纹包含实际工具版本。

| 线上状态 | 文件数 |
| --- | ---: |
| failed | 210 |
| unsupported | 4,344 |
| optimized | 3,383 |
| unchanged | 26,294 |
| no-gain | 342 |
| pending | 39,386 |

unsupported 包含 3,654 个 catalog 结构检查、534 个注释检查和 156 个加密检查。
其中仅含 `/StructTreeRoot` 的有 1,208 个，仅含 `/AcroForm` 的有 707 个；这是旧校验器
首先命中的原因，不保证移除第一道限制后就没有其他限制。

failed 分组为：PDF.js worker 模糊异常 152、旧版 qpdf 警告退出 25、内容签名不同 17、
其他候选失败 13、qpdf 严重错误 2、页面树循环 1。152 个 worker 异常中只抽取两份
复现，均是目录引用没有指向页面，不能据此断言其他 150 份也是同一原因。

完整逐文件清单可只读重新生成：

```bash
python3 -B scripts/audit_pdf_range_failures.py \
  --revision 33db8c051a01182c1ed0ec567f37d8da240f2e8a \
  --output output/pdf-range-failures.json
```

省略 `--revision` 查询当时的最新线上版本；`--manifest` 可读取本地 canonical manifest。
审计只读取状态，不下载 PDF、不改动线上数据。

## 已解决的检查缺口

- XMP 元数据：qpdf 会把压缩 XML 解压，即使 `--stream-data=preserve` 也可能如此。
  原算法比较压缩表示，误判为内容变化；现在比较精确解码内容。图片、字体、内容流仍比较
  原始字节。元数据真实修改仍被回归测试拒绝。
- 目录坏链接：以前 PDF.js 的普通 worker 对象经过 Playwright 后只留下
  `UnknownErrorException`。现在保留具体 message/details；仅对目录里的已知非页面引用
  记录位置后继续测量。完整目录图和坏链接位置都必须保持相同。
- 标签、注释、普通表单、命名目录、输出色彩与可选内容：增加保留循环和共享引用的完整对象图
  比较，使用稳定编号而非原 PDF 对象号。测试实际改变了标签文本/所属页面、批注、表单值、
  命名跳转页，均可检测。普通字典 `/Length` 的变化也不再被忽略。
- 加密：仅允许能以空密码打开的文件进入候选评测，并保留加密和权限。AES-128 fixture 的
  三种 qpdf 候选验证通过；移除加密会签名不等，需要真实打开密码仍拒绝。某些加密版本的
  qpdf 输出会新增扩展声明，仍可能因结构不等而不能选用，不宣称全部 156 份均可处理。
- 重试与发布：新增 `retry_blocked` 定向选择失败/受限文件；修复规则身份升级被发布器
  误当作过期结果跳过的问题。重试清除旧候选、旧错误、旧输出路径，避免新结果混入旧状态。
- 索引重建备用路径：当普通候选均未通过且 qpdf 出现严重错误时，对未加密输入用 pypdf
  在限时子进程中独立重建。中间文件先过 qpdf 和完整图比较，随后最终候选再次与原件
  比较。测试同时验证可恢复情形及重建意外改变页面后必须拒绝的情形。

## 已生成的本地优化样本

选取 22 份来自固定来源 revision 的真实文件：8 份 optimized、6 份 unchanged、
1 份 no-gain、6 份 unsupported、1 份 failed。下表八份都通过 qpdf 检查、全页结构/资源
签名、PDF.js 抽样像素和文字比较以及读取收益门槛。七份是直接对象流候选，
《民国外债档案史料06》是通过无损重建后生成的对象流候选。
这里的“启动”包含首页、附近页面及目录读取，是本地 Range 字节量，不是线上耗时。

| 文件 | 启动字节：原件 → 候选 | 降幅 |
| --- | --- | ---: |
| 帝国与民族主义的战争.pdf | 6,181,700 → 1,764,771 | 71.5% |
| 民国外债档案史料06.pdf | 12,139,581 → 1,662,463 | 86.3% |
| 马克思恩格斯全集旧版：33 书信 1870.7-1874.pdf | 4,401,059 → 1,729,421 | 60.7% |
| 新聞 2020.12.15.pdf | 4,615,586 → 2,309,496 | 50.0% |
| 论法的精神（大字版）.pdf | 4,300,797 → 2,572,474 | 40.2% |
| 马克思恩格斯全集旧版：27 书信 1842.2-1851.12.pdf | 4,205,398 → 1,393,066 | 66.9% |
| 斯大林：战后（第一册）1945-1948.pdf | 4,196,703 → 2,420,799 | 42.3% |
| 政治局和中央委员会秘书处：1945-1985年.pdf | 4,224,940 → 1,979,202 | 53.2% |

`学习《资本论》提要 初稿 第1卷` 和 `《毛泽东选集》第5卷 名词解释` 的目录坏目标
曾使整本评测报错；现在两份都完成评测并记录 unchanged，原版启动读取量已经达标。
俄语 1918 年苏俄宪法、斯大林全集第十三卷、斯大林的孩子们这三份含标签/表单/注释的样本
也完成评测，结果 unchanged。`Asking the Right Questions` 完成评测，结果 no-gain。
马克思恩格斯选集林一章版第 1 卷也为 unchanged。《政治局和中央委员会秘书处》的无效
间接引用被解析为 null；按 PDF 字典 null 等于缺省的语义比较后，候选完整通过。

《民国外债档案史料06》原件有 631 页，qpdf 直接重建会选错根对象，输出被 PDF.js 报为
`Invalid Root reference`；pypdf 重建后图签名完全相同、qpdf 检查无错误，最终候选通过
与原件的全部验证。该实例证明一种可修复的索引损坏，不代表任意页面树循环均可修复。

结构比较实际检测到 `斯大林：战后` 的线性化候选改变了命名目录相关对象，拒绝该候选；
同文件的对象流候选保持完整结构并通过收益门槛。不能仅凭 qpdf 成功、页面能显示就接受候选。

## 仍需保留原件或另行修复的情况

| 原因 | 已看到的情况 | 后续处理 |
| --- | --- | --- |
| 页面内容流损坏 | `苏俄的性别与住房问题：公共空间中的私人生活.pdf` 第 49 页 Flate 内容流解码错误 | 寻找完整来源或逐页人工核对；单纯重排无法补回丢失数据 |
| 页面树循环 | 线上另有 1 份 `Detected cyclic page references` 记录，本次未抽取其源文件 | 需先确定可信页数与顺序；当前无法建立完整原件签名，不进入备用重建路径 |
| 数字签名 | 字节范围签名依赖原件字节 | 原签名文件保留；若需要预览衍生版，应作为独立资源明确标识，不能视为签名仍有效 |
| 打开密码 | 空密码不能读取 | 使用已知密码的独立转换流程；不猜测密码 |
| XFA/自动动作/外部动作/附件/私有扩展 | 部分样本越过旧 catalog 限制后仍命中这些结构 | 继续保留；需要各自的行为和资源完整性检查，不能简单删除 |
| 无足够收益 | 结构相同但读取量降幅不足、跳页变差等 | 保留原件，记录 no-gain；不反复自动重跑 |

图片流可作为阅读预览的另一条路线，但不能等价保留数字签名、交互表单、附件或标签语义。
本次不把这些 PDF 自动改成图片，也不增加 OCR。

## 验证与回填

快测命令见 `pdf_range_workflow.md`；包含真实 qpdf fixture、加密权限、复杂结构变异和
发布身份竞争测试，本次 217 项通过。浏览器实测是显式样本验收，不加入默认快测。

本机调查目录为 `/tmp/opencode/pdf-range-investigation/`：`failures.json` 是完整线上失败/受限
清单，`sample-results.json` 是样本诊断，`validated-bundle/` 含 22 条新评测结果及 8 份
已校验产物。bundle 可交给分片发布器，结果携带 `_previous_identity`，远端身份改变时会
跳过旧结果。文件复制入 bundle 时再次验证 SHA-256、长度及当前完整图签名。

修复发布后可用工作流 `retry_blocked=true` 运行有限批次，或 CLI
`--retry-blocked --limit 10 --workers 2 --build-only` 先生成候选。新规则版本使旧身份进入
重评；定向重试仅选择旧 failed/unsupported。发布后需要再次查询固定线上 revision 的
状态、校验 sidecar 与 Reader/Range 入口，不能以这份本地报告代替部署验收。

## 上线验收（2026-09-16 UTC）

- 修复提交：`anftm/pipeline@6ef6128d9577b7e40007df65fb2a20e4923bb62a`。
  独立发布检出与远端基线逐文件核对后测试 218 项通过；原工作区 Git worktree 引用损坏，
  因而没有在该工作区改写历史。远端随后合入的电子书章节变更保留。
- 已验证的 22 条结果及 8 份产物发布至 Reader-Assets
  `26747b64356e7dc35171946ade8f441d420e23fe`，当前源文件指纹、8 条映射变化及远端结果均核对通过。
- 定向重试运行 `https://github.com/anftm/pipeline/actions/runs/35152997679`，
  参数 `retry_blocked=true, limit=10, checkpoints=1`，15 个分片共 150 份。
  首次一个 Runner 安装依赖遭遇 Microsoft apt 源 403，重跑失败任务后整个工作流成功。
  结果：60 optimized、66 unchanged、3 no-gain、20 unsupported、1 failed；发布 150 条，跳过 0 条。
- 重试产物发布至 Reader-Assets `2b1222d25fbed64acbdc96d2f2c492176a624bc3`。
  独立比较前后 manifest，恰好 150 条变化，全部来自旧 failed/unsupported；60 份新产物的
  远端 SHA-256、大小与映射逐条一致。本轮累计新增优化产物为 68 份。
- GitHub Pages 最终映射提交 `885bbe9abe1f08bc851483b88823edc3879afaa6`，部署运行
  `https://github.com/vomebook/search/actions/runs/35153793505` 成功。
  实际线上 sidecar 与固定 HF revision 的字节完全相同，SHA-256 为
  `5256b5bba75d64a70ef68a5779021fc2f1090b81446a554eb2b4180bc117abba`。
- 最初 8 份产物的线上 Reader resolve、原始下载链接、首尾各 1024 字节 Range 全部通过；
  重试新增产物又抽查 2 份，结果相同。两站各实测《民国外债档案史料06》（631 页）和
  《论法的精神（大字版）》（1,735 页）的首页、第 10 页、中间页、末页，无页面脚本异常。
  HF 生产冒烟 4 项通过，GitHub Pages 生产冒烟通过。

最终固定 revision `2b1222d...` 的全库状态如下，后续定时工作流可能继续改变计数：

| 状态 | 文件数 |
| --- | ---: |
| optimized | 3,451 |
| unchanged | 26,366 |
| no-gain | 346 |
| unsupported | 4,208 |
| failed | 202 |
| pending | 39,386 |

本批剩余失败为 `VoiceOfML/A-Historical-Learning-Data` 的
`苏俄（联）革命/革命联盟《资本主义是怎样在苏联复辟的》.pdf`：对象流候选内容签名不同，
其他候选未通过收益门槛，继续保留原件。尚未完成所有失败/受限文件的全库重试。
本机 `final-publication-verification.json`、`retry-results.json`、`remaining-failures.json`
和 `workflow-results.json` 保存于上述调查目录，可复核本批结果及剩余逐文件清单。

## 后续回填与疑难样本（2026-09-16 UTC）

连续回填增加 `retry_stale_blocked`，按输入、规则、工具身份排除已经重评的结果。
针对队列推进、失败重试、输入变化的 31 项 PDF 快测通过。提交 `9a6af2a` 已上线；
运行 `https://github.com/anftm/pipeline/actions/runs/35155084024` 使用
`retry_stale_blocked=true, limit=100, checkpoints=3`，15 个 Runner 总容量 4,500。
提交时状态为 pending，等待现有 Build Reader Assets 共享锁；不能把排队视为完成。

固定 Reader-Assets revision `648ed93e56c033cd7340b411c8adf43f05e1e8d7` 中，失败/受限
仍为 4,410 条。按上一批 Runner 的 `qpdf version 11.9.0` 和状态内输入指纹规划，21 条
已使用当前规则、4,389 条需要重评。工作流开始时重新发现源文件与生成产物，实际队列
仍以 Runner 当时的输入身份为准。本机 `continuation-queue.json` 保存规划依据。

补充调查了两份真实源文件（本机 qpdf 10.6.3）：

- `革命联盟《资本主义是怎样在苏联复辟的》.pdf`：368 页；对象流候选的图差异为加密
  过滤器 `/CFM` 从 `/V2`（RC4）变为 `/AESV2`。`--copy-encryption` 和
  `--allow-weak-crypto` 两个诊断方案均未保持原算法。当前精确保留加密参数的约束下
  继续拒绝，不把它误报成页面内容损坏，也不放宽加密校验。
- `000516/1870520043_6115_马克思__恩格斯__列宁论社会主义经济.pdf`：页面树声明
  907 页，qpdf/pdfinfo 也报告 907；直接树遍历得到 1,810 个叶引用，仅 942 个不同
  页面对象，存在重复引用。qpdf 重写后 pypdf 仍报循环页面引用，并有内容流结构警告。
  不能据此任意去重或截取前 907 页，需要可信源版本确认页数及页序。

原件、qpdf 候选、结构差异与页面树诊断保存在 `/tmp/opencode/pdf-recovery-followup/`。

## 吞吐调查与修复（2026-09-17 UTC）

对比 `2b1222d25fbed64acbdc96d2f2c492176a624bc3` 和
`f63cbb6eb046bdfc97458421e6968838178d467c`，七次成功工作流各发布 1,800 条，
共 12,600 个不同记录发生变化，全部来自既有终态记录。原 39,386 个 pending 没有推进。
这是升级规则后旧结果重评与从未评测输入共用按路径排序队列造成的饥饿，不应只用
optimized 的增加量衡量计算次数，也不能宣称这段时间在持续推进新文件。

运行 `35188653284` 的十四个分片于 06:20–06:30 完成，第五分片从 06:12 卡到
11:42 的 330 分钟 Runner 超时，最终 publish 被跳过。已有子工具超时未覆盖 pypdf
签名和浏览器清理；单个异常文件拖住整批及共享锁。GitHub 默认 concurrency 仅保留
一个 pending，`cancel-in-progress: false` 也不会保留所有待运行任务；之前的手动
续跑 `35155084024` 在等待时被替换。另有调度空档，不能把 13 小时视为连续计算时间。

修复提交 `92985d5`：

- 首次评测与旧结果重评交替取任务，两组均非空时各约一半；重评内优先失败/受限。
- PDF 计算使用独立工作流队列，共享 reader-assets 锁仅由发布 job 持有；相关发布入口
  全部使用 GitHub 已支持的 `queue: max`，保留最多 100 个待执行任务。
- 每份评测使用独立进程组，总超时 900 秒，超时终止进程组并继续。
- 每份完成结果原子落盘，异常分片仍上传已完成的结果；发布合并可用分片。
- 手动 `followups` 给出 0..20 的后续批次数预算，每次成功发布后递减并续跑，空批次停止。
- 制品不再携带每分片约 157 MB 的完整状态副本或临时目录，只上传结果和最终产物。

相关测试 225 项通过；真实 305 页样本在隔离进程中完成全套评测并仍选中 objects。
固定线上输入规划证明 1,800 个额度中 900 个能分给 pending。上线验收先运行 150 个
额度并验证一次自动续跑，再启动较大的有界连续回填；实际进度以远端 manifest 差分为准。

线上验收结果：

- `35231921321` 于 14:11–14:19 完成，发布 150 条、跳过 0 条；其中原 pending 75 条，
  结果为 72 optimized、3 unchanged。HF revision `49cb978f384a5aa91b5c7890cbfb6fb549188b2e`。
- 前一批成功后自动触发 `35232773990`，同样发布 150 条、跳过 0 条；原 pending 75 条，
  结果为 30 optimized、32 unchanged、10 unsupported、3 no-gain。HF revision
  `81a37655dc6cb7e2f5463cbae737279e4ab62c98`。两批合计推进 150 条首次评测，待处理
  39,386 → 39,236，optimized 总量 4,743 → 4,846。重评产物不重复计作新增优化。
- 两批候选的远端大小、LFS SHA-256、映射逐条验证；两份首次优化样本 `PR1963-08.pdf`
  和 `PR1962-46.pdf` 在 HF/Pages 均通过首页、第 10 页和末页渲染及首尾 Range 检查。
- Pages 最终部署 `35233424643` 成功；映射字节与上述 HF revision 一致，SHA-256 为
  `1cb8d5a6a7ea3e163144ec7e0b2a212f4e48d51d431e664acfb499976c3b447d`。
- 大批量运行 `https://github.com/anftm/pipeline/actions/runs/35233011764` 已进入 build，
  参数 `limit=100, checkpoints=4, followups=9`：每批上限 6,000，含当前批最多 10 批。
  后续只在成功发布后续跑，空结果或预算用完停止；此记录不表示大批量回填已完成。

完整依据保存在本机 `throughput-audit.json`、`throughput-acceptance-*.json` 和
`throughput-results-*.json`；下载的一次本地 manifest 解析失败经重新读取、远端 SHA-256
核对及二次差分验证排除，不将其归为线上生成数据损坏。
