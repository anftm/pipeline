name: Schedule PDF Assets

on:
  schedule:
    - cron: "17,47 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Dispatch worker only when idle
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          test -n "${GH_TOKEN}"
          runs=$(curl --fail-with-body --silent --show-error \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${REPO}/actions/workflows/pdf-assets-worker.yml/runs?per_page=20")
          active=$(printf '%s' "${runs}" | jq '[.workflow_runs[] | select(.status == "queued" or .status == "in_progress")] | length')
          if ((active > 0)); then
            echo "PDF worker already active; no pending run will be created."
            exit 0
          fi
          curl --fail-with-body --silent --show-error -X POST \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            --header "Content-Type: application/json" \
            --data '{"ref":"main"}' \
            "https://api.github.com/repos/${REPO}/actions/workflows/pdf-assets-worker.yml/dispatches"
